import 'package:flutter/material.dart';

class ExploreScreen extends StatelessWidget {
  const ExploreScreen({super.key});

  @override
  build(BuildContext context) {
    return const Center(child: Text('Explore 페이지'));
  }
}
