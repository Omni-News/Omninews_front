import 'package:flutter/material.dart';

class RssScreen extends StatelessWidget {
  const RssScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Rss 페이지'));
  }
}
